# 9. 최우선 수정사항 TOP 50 (보안·인증 반영 최종 순위)

> 출처: `polymorphic-greeting-gizmo.md` §9

## Tier 0 — 보안 봉합 + 제품 성립 (지금 안 하면 나머지 무의미. 보안이 기능보다 앞선다)

1. **C-1 봉합**: `profiles` UPDATE에 `WITH CHECK`(또는 `role`/`total_earnings`/`pending_payout` 컬럼 권한 REVOKE + SECURITY DEFINER RPC). 권한 자가승격 차단 — 최우선.
2. **C-2 해소**: 라이브 DB `pg_dump` → 실제 스키마 커밋, `role` 컬럼 정본화. 소스=프로덕션 재현 가능 상태 복구.
3. **H-1 봉합**: `collab_sessions` `SELECT USING(true)` → host/participant 스코프 또는 invite_code 필터 RPC. 초대코드 덤프 차단.
4. 정적 export 결별: Vercel(Next 서버/ISR) 이전 또는 SPA catch-all 라우팅으로 UGC URL 서빙. (근본 원인)
5. `share/[shareId]`·`collab/[code]`·`creator/[username]`·`templates/[id]` 동적 서빙 복구(404 제거) + C-5 협업 join을 Supabase 경로로 배선.
6. 결제 백엔드(Toss 서버 라우트/웹훅) — 4와 묶어 해결.
7. **C-3/C-4 클라이언트 신뢰 제거**: 구독·구매·수익·정산을 서버(RLS 강제 테이블/Edge Function)서 검증. localStorage는 캐시로 강등.
8. H-5(env 누락 시 빌드 하드실패 + 데모모드 UI 배너), H-4(Supabase Auth 리밋/Turnstile), H-3(죽은 서버 인가 코드 제거).

## Tier 1 — 신뢰·성능·정합

9. CI에 test+lint 게이트 추가(deploy.yml — 현재 build만).
10. 키입력·슬라이더 히스토리 push 디바운스(공용 debounce + 커밋 단위 스냅샷).
11. CanvasEditor 셀렉터 구독 전환 + 6개 Konva 렌더러 memo + 핸들러 useCallback.
12. useUser.ts 이메일 등 프로덕션 console 제거·가드.
13. 가격/수수료 단일 정본화(현 15%/30%/20%·₩3,900/4,900/9,900 모순 해소).
14. 폰트: Google Fonts **self-host 전환**(런타임 의존·PII·CSP 완화) + 폰트 팩 확대 + UI/에디터 폰트 통합. (자산 0 아님 — 정정)
15. persist 스토어 version/migrate 도입.
16. 죽은 데이터 경로 제거(sampleResources→useTemplates 연결); M-2 템플릿 업로드 실배선.
17. border-radius 무한 keyframe 4종 → transform 기반 대체/제거.
18. 공용 `useMediaQuery` 훅 + reduced-motion 단일 반응형 구현.
19. **인증 최소 PII(DL-0002)**: 소셜(구글/카톡/네이버) 제거 or 플래그화 + 이메일 매직링크(`signInWithOtp`) 경로 + profiles 불필요 PII 컬럼 정리 + CSP `unsafe-eval` 규명·제거.
20. **"완성-미배선" 즉시 연결(저비용 고가치)**: 테마 초기화 mount(다크모드 실작동 복구) + `canCreateMore` 함수 오용 버그 수정(캐릭터 제한 복구) + Whisper 훅 2중 통합 + 마켓 `recordSale` 배선 or 제거 + 자료 읽기 경로.

## Tier 2 — 구조·범위 정리 (19~50)

god-component 분해(CanvasEditor/EditorSidebar), 스코프 재확정(게이미피케이션·Duo 존치/제거), 통신판매중개자 컴플라이언스, 신고/관리자 도구, 워터마크/도난방지 실구현, 가상화 도입, index-key 교체, `claude-code` devDeps 이동, a11y 테스트(jest-axe), 대화면 대응, M-3 whisper 상태전이 트리거, M-5 스토리지 버킷 RLS 커밋, storage 경로 소유권 검증 …

*(전체 50항목 표는 실행 시 `docs/audit-2026-07-05/top50.md`로 상세화 — 각 항목에 파일:라인·수용기준·모델라우팅 태그[Fable/Opus/Sonnet/Haiku] 부여)*
