# F-27 구매 플로우(usePurchase)   [stub(no pay)]

**WHAT**: 유료 자료/템플릿 개별 구매. `src/hooks/usePurchase.ts`(117줄).

**현재상태**: stub(FACT). 72행 주석 "데모 모드: 실제 결제 없이 바로 구매 처리". 71-87행대 구매 플로우 전체가 시뮬레이션 — 76행 `await new Promise((resolve) => setTimeout(resolve, 1000))`로 인위적 지연만 넣고, 78-86행대 `storePurchase()`를 결제 게이트웨이 호출 없이 직접 실행. Stripe/Toss 등 실 결제 프로바이더 연동 0건.

**스펙정합**: 07-api-design.md의 결제 API 스펙 대비 완전 미구현 — 이 기능은 사실상 "구매 버튼을 누르면 1초 후 무조건 성공"인 상태.

**문제·리스크**: **Critical** — 실제로 돈을 받지 않고 구매를 확정 처리 — 매출 라인 0원(감사 §1 치명 테마 #2). 결제 웹훅이 정적 export 환경에서 원천 불가하다는 아키텍처 제약과 직결.

**Fable판정**: **결제백엔드 의존** — 이 기능은 독립적으로 고칠 수 없음. Toss 등 실 결제 프로바이더의 서버 라우트·웹훅이 먼저 구축돼야 함.

**다음작업**:
1. Toss Payments(또는 선정된 PG사) 서버 라우트+웹훅 구축(정적 export 결별과 함께, TOP50 #6) — AC: 실 결제 승인 후에만 `storePurchase()` 호출. (Opus)
2. 1번 완료 후 usePurchase.ts:72-87을 실 결제 API 호출로 교체, setTimeout 시뮬레이션 제거. (Opus)

**의존·순서**: **선행조건**: 정적 export 결별 + 결제 백엔드(TOP50 #4·#6, 서로 묶어 해결 권장 — 감사 원문에 "4와 묶어 해결" 명시). F-24(마켓플레이스)·F-28(프리미엄 게이팅)과 동일 선행조건 공유 — 이 셋은 사실상 하나의 대형 작업(결제 백엔드 구축)의 하위 조각.
